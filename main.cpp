/* 
	ShiftingBricks
 */

#include <sifteo.h>

static Metadata M = Metadata()
    .title("ShiftingBricks")
    .package("com.sifteo.sdk.text", "1.0")
    .icon(Icon)
    .cubeRange(NUM_CUBES);

void main {

	static Game game;
	
	while(1){
		/*
		Run all Game functions
		1. titlescreen / init
		2. game
		3. game over
		4. cleanup method
		*/
	}
}